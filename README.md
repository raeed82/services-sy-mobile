# تطبيق منصة الخدمات السورية - Mobile

تطبيق React Native مع Expo لمنصة الخدمات السورية يعمل على iOS و Android.

## 🚀 الميزات

- ✅ **نظام Fallback الذكي**: الاتصال بـ Manus أولاً، وفي حالة التعطل الاتصال بالسيرفر الخارجي تلقائياً
- ✅ **تسجيل الدخول والتسجيل**: مصادقة آمنة مع حفظ البيانات محلياً
- ✅ **عرض الخدمات**: قائمة شاملة بجميع الخدمات المتاحة
- ✅ **البحث والتصفية**: البحث بالكلمات المفتاحية والتصفية حسب الفئة والموقع
- ✅ **الملف الشخصي**: عرض وتعديل بيانات المستخدم
- ✅ **إدارة الخدمات**: إنشاء وتعديل وحذف الخدمات
- ✅ **التواصل**: التواصل المباشر مع مقدمي الخدمات

## 📁 هيكل المشروع

```
services-sy-mobile/
├── app/
│   └── App.tsx                 # الملف الرئيسي للتطبيق
├── src/
│   ├── api/
│   │   └── client.ts          # عميل API مع نظام Fallback
│   ├── components/            # المكونات المشتركة
│   ├── screens/               # شاشات التطبيق
│   ├── hooks/                 # Custom Hooks
│   │   ├── useAuth.ts         # Hook للمصادقة
│   │   └── useServices.ts     # Hook للخدمات
│   ├── store/                 # Zustand Store
│   │   ├── authStore.ts       # Store المصادقة
│   │   └── servicesStore.ts   # Store الخدمات
│   └── utils/                 # دوال مساعدة
├── assets/                    # الصور والأيقونات
└── package.json              # المكتبات والإعدادات
```

## 🔧 التثبيت والتشغيل

### المتطلبات
- Node.js v18+
- npm أو pnpm
- Expo CLI

### التثبيت

```bash
cd services-sy-mobile
npm install
# أو
pnpm install
```

### التشغيل

```bash
# تشغيل على جميع المنصات
npm start

# تشغيل على Android
npm run android

# تشغيل على iOS
npm run ios

# تشغيل على الويب
npm run web
```

## 🔌 نظام Fallback

التطبيق يحتوي على نظام ذكي للتعامل مع تعطل الخوادم:

1. **محاولة الاتصال بـ Manus أولاً** (الخادم الأساسي)
2. **إذا فشل الاتصال** → الاتصال بالسيرفر الخارجي تلقائياً
3. **إذا عاد الاتصال بـ Manus** → العودة إليه تلقائياً

```typescript
// مثال على الاستخدام
import apiClient from './src/api/client';

// الطلب يتم تلقائياً مع نظام Fallback
const data = await apiClient.get('/api/trpc/services.list');

// التحقق من حالة الاتصال
const status = apiClient.getConnectionStatus();
console.log(status);
// {
//   current: "https://3000-...",
//   primary: "https://3000-...",
//   fallback: "http://95.169.192.233",
//   isUsingFallback: false
// }
```

## 📱 الشاشات الرئيسية

### 1. شاشة تسجيل الدخول
- تسجيل الدخول بالبريد الإلكتروني وكلمة المرور
- رابط للتسجيل الجديد
- حفظ البيانات محلياً

### 2. الرئيسية
- عرض الخدمات الأخيرة
- التنقل السريع للفئات
- البحث السريع

### 3. البحث
- البحث المتقدم بالكلمات المفتاحية
- التصفية حسب الفئة
- التصفية حسب الموقع
- عرض النتائج

### 4. خدماتي
- عرض الخدمات الخاصة بالمستخدم
- إنشاء خدمة جديدة
- تعديل الخدمات
- حذف الخدمات

### 5. الملف الشخصي
- عرض بيانات المستخدم
- تعديل الملف الشخصي
- تسجيل الخروج

## 🔐 المصادقة

التطبيق يستخدم نظام مصادقة آمن:

```typescript
import { useAuth } from './src/hooks/useAuth';

function MyComponent() {
  const { user, login, logout, isAuthenticated } = useAuth();
  
  const handleLogin = async () => {
    await login('email@example.com', 'password');
  };
  
  return (
    <View>
      {isAuthenticated ? (
        <Text>مرحباً {user?.name}</Text>
      ) : (
        <Button title="تسجيل الدخول" onPress={handleLogin} />
      )}
    </View>
  );
}
```

## 🛠️ إدارة الحالة

التطبيق يستخدم Zustand لإدارة الحالة:

```typescript
import { useAuthStore } from './src/store/authStore';
import { useServicesStore } from './src/store/servicesStore';

// Store المصادقة
const { user, token, logout } = useAuthStore();

// Store الخدمات
const { services, filteredServices, searchServices } = useServicesStore();
```

## 📦 البناء والنشر

### بناء APK لـ Android

```bash
npm run build:android
```

### بناء IPA لـ iOS

```bash
npm run build:ios
```

### بناء كلا المنصتين

```bash
npm run build:all
```

## 🧪 الاختبار

```bash
npm test
```

## 📝 الملاحظات

- التطبيق يستخدم نفس API الموقع (tRPC)
- جميع البيانات محفوظة محلياً باستخدام AsyncStorage
- النظام يدعم اللغة العربية بشكل كامل
- التطبيق متوافق مع iOS 12+ و Android 5+

## 🤝 المساهمة

للمساهمة في المشروع، يرجى اتباع الخطوات التالية:

1. Fork المشروع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

## 📞 التواصل

للأسئلة والاستفسارات، يرجى التواصل عبر:
- البريد الإلكتروني: info@services-sy.com
- الموقع: https://services-sy.com

---

**ملاحظة مهمة**: هذا التطبيق منفصل تماماً عن الموقع الأساسي ولا يؤثر عليه بأي شكل.
