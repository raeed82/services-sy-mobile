# 🚀 بناء تطبيق منصة الخدمات السورية

## الطريقة السريعة (بدون Expo)

### المتطلبات:
- Node.js 18+
- npm أو yarn
- Android Studio (اختياري، فقط لـ APK)

### الخطوات:

#### 1️⃣ تثبيت المكتبات
```bash
cd services-sy-mobile
npm install
```

#### 2️⃣ بناء APK (للاختبار على الهاتف)
```bash
npm run android
```

#### 3️⃣ بناء AAB (للنشر على Google Play)
```bash
npm run build:android
```

---

## الطريقة المتقدمة (مع Expo)

### 1️⃣ تسجيل الدخول إلى Expo
```bash
eas login
# أدخل بيانات حسابك:
# البريد: raeedrd@gmail.com
# اسم المستخدم: raed_services_sy
# كلمة المرور: Aifx@Has999
```

### 2️⃣ بناء AAB عبر السحابة
```bash
eas build -p android
```

### 3️⃣ النشر على Google Play
```bash
eas submit -p android
```

---

## ملفات مهمة:

- `app.json` - إعدادات التطبيق
- `eas.json` - إعدادات البناء
- `package.json` - المكتبات والإعدادات
- `app/App.tsx` - الملف الرئيسي

---

## معلومات التطبيق:

- **الاسم**: منصة الخدمات السورية
- **الإصدار**: 1.0.0
- **الحزمة**: com.services_sy.mobile
- **اللوغو**: assets/icon.png
- **Splash**: assets/splash.png

---

## نصائح مهمة:

1. ✅ تأكد من توفر 5 GB مساحة حرة
2. ✅ استخدم اتصال إنترنت سريع
3. ✅ لا تغلق الطرفية أثناء البناء
4. ✅ البناء الأول قد يستغرق 30-60 دقيقة

---

## حل المشاكل:

### مشكلة: "npm ERR!"
**الحل:**
```bash
npm cache clean --force
npm install
```

### مشكلة: "Java not found"
**الحل:**
```bash
# تثبيت Java
sudo apt-get install openjdk-11-jdk
```

### مشكلة: "Gradle build failed"
**الحل:**
```bash
cd android
./gradlew clean
cd ..
npm run android
```

---

## الملفات الناتجة:

- **APK**: `android/app/build/outputs/apk/release/app-release.apk`
- **AAB**: `android/app/build/outputs/bundle/release/app-release.aab`

---

## النشر على Google Play:

1. اذهب إلى: https://play.google.com/console
2. أنشئ تطبيق جديد
3. حمّل ملف AAB
4. أضف الصور والوصف
5. انشر التطبيق

---

**تم إنشاء هذا الملف في: 2026-02-10**
